%h3 Album Show

%p
  Album Title :
  = @album.title

%p
  Album Description :
  = @album.description
  %br


  /Must show just current user albums

  = link_to "View all Albums", albums_path
  \|
  - if can? :edit, @album
    = link_to "Edit Album", edit_album_path(@album)


    %h3 Form to Add photo

    %ul.errors
      - @photo.errors.full_messages.each do |error|
        %li= error
    = form_for(@photo) do |f|
      = hidden_field_tag :album_id, @album.id
      = f.label :title
      = f.text_field :title
      = f.label :description
      = f.text_field :description
      = f.file_field :image
      .input-row
        = f.label :tags
        = f.text_field :tag_list, id: "photo_tags_list"
      = f.submit
%br

%h3 photos in album
/@photos
/ - @photos_ordered = @album.photos.sort_by{ |post| post.votes_for.size }.reverse
- @photos.each do |photo|
  %ul
    %li
      = link_to image_tag(photo.image_url(:thumb), :width => "400px"), photo_path(photo)
    %li
      = photo.title
    %li
      = photo.description
    %li
      = photo.tag_list
  - if can? :destroy, photo
    = link_to "Remove photo", photo_path(photo), method: :delete
  %hr

%h3 Album owner
= @album.user.name

%br

= link_to "View all Albums", albums_path
\|
- if can? :manage, @album
  = link_to "Edit Album", edit_album_path(@album)
  \|
  = link_to "Delete Album", album_path(@album), method: :delete
  \|
/ = link_to "Back to user profile", user_path(@album.user_id)



