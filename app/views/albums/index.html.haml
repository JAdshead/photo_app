%h3 Albums Page

= link_to "Create new album", new_album_path

- @albums.each do |album|
  %ul
    %li
      %h4= album.title
    %li
      = album.description
    %li
      = album.user_id
    = link_to "View Album", album_path(album)
    \|
    - if current_user.role?(:admin) || album.user == current_user
      = link_to "Edit Album", edit_album_path(album)
